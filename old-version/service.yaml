apiVersion: v1
kind: Service
metadata:
  name: reddit-clone-app-service
  labels:
    app: reddit-clone-app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: externaldnslb.reankloud.site
    service.beta.kubernetes.io/aws-load-balancer-type: external
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  selector:
    app: reddit-clone-app

  ports:
    - port: 3000
      targetPort: 3000

  type: LoadBalancer
