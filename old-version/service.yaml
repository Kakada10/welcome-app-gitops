apiVersion: v1
kind: Service
metadata:
  name: welcome-app-service
  labels:
    app: welcome-app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: welcome-prod.reankloud.site
    service.beta.kubernetes.io/aws-load-balancer-type: external
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  selector:
    app: welcome-app

  ports:
    - port: 3000
      targetPort: 3000

  type: LoadBalancer
