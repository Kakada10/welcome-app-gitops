apiVersion: apps/v1
kind: Deployment
metadata:
  name: welcome-app-deployment-qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: welcome-app-qa
  template:
    metadata:
      labels:
        app: welcome-app-qa
    spec:
      containers:
        - name: welcome-app
          image: capybara22/welcome-app:qa-1
          resources:
            limits:
              cpu: "1"
            requests:
              cpu: "500m"
          ports:
            - containerPort: 3000
